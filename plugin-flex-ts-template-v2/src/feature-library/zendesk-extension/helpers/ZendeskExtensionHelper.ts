import { ITask, Manager, Notifications } from '@twilio/flex-ui';

import ZendeskExtensionService from './ZendeskExtensionService';

import logger from '../../../utils/logger';

const manager = Manager.getInstance();

export const setAssignee = async (task: ITask): Promise<boolean> => {

    return false;
}